[Unit]
Description=Back up the MachReach database every night

[Timer]
# 04:30 UTC is 00:30 or 01:30 in Santiago, after the midnight plan refresh.
OnCalendar=*-*-* 04:30:00
Persistent=true

[Install]
WantedBy=timers.target
